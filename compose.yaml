services:
  rou:
    image: ghcr.io/cptsamfalcon/rou:latest
    container_name: rou
    restart: unless-stopped
    env_file: .env
    environment:
      TZ: America/Los_Angeles
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

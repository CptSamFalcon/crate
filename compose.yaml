services:
  rou:
    image: ghcr.io/cptsamfalcon/rou-bot:latest
    container_name: rou-bot
    restart: unless-stopped
    network_mode: host
    # Dashboard binds to 127.0.0.1:WEB_PORT (default 8787). The tunnel reaches it there.
    env_file: .env
    environment:
      TZ: UTC
      NODE_OPTIONS: --dns-result-order=ipv4first
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
  tunnel:
    image: cloudflare/cloudflared:latest
    container_name: rou-tunnel
    restart: unless-stopped
    network_mode: host
    depends_on:
      - rou
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    command: tunnel --no-autoupdate run
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

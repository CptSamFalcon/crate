services:
  rou:
    image: ghcr.io/cptsamfalcon/rou-bot:latest
    container_name: rou-bot
    restart: unless-stopped
    env_file: .env
    environment:
      TZ: UTC
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
